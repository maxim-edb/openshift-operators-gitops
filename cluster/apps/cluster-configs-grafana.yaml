apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-configs-grafana
  namespace: openshift-operators
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    directory:
      recurse: false
    path: cluster/grafana
    repoURL: https://github.com/maxim-edb/cluster-config.git
    targetRevision: dev
