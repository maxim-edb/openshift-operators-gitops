apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-configs-pgadmin4
  namespace: openshift-gitops
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    path: cluster/pgadmin4
    repoURL: https://github.com/maxim-edb/cluster-config.git
    targetRevision: rosa
  syncPolicy:
    automated: {}
