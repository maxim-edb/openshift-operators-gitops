apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: minio-client
  namespace: minio
spec:
  host: minio-client.sno.thuisnet.lan
  path: /
  port:
    targetPort: 9000
  to:
    kind: Service
    name: minio-client
